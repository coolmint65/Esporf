services:
  esporf-discord:
    build: .
    container_name: esporf-discord
    restart: unless-stopped
    env_file: .env
    command: ["discord"]
    stop_signal: SIGINT
    stop_grace_period: 15s
    volumes:
      - esporf-data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  esporf-data:
